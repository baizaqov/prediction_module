# Задачи разработки

Редакция 2. Составлена от фактического состояния кода (ветка `origin/test`, коммит `bff4e0b`)
по [roadmap.md](roadmap.md). **Первая редакция аннулирована** — она предполагала разработку с нуля,
и часть её задач уже реализована.

**Нумерация начата заново.** Прежние `TASK-101…TASK-1202` больше не используются.

| Статус | Значение |
|---|---|
| `READY` | Можно брать в работу, ответов бизнес-аналитика не требуется |
| `PARTIAL` | Можно начинать, но часть объёма ждёт ответа |
| `BLOCKED` | Брать нельзя до ответа |
| `EXTERNAL` | Не задача разработки |

**Итог:** 18 задач. `READY` — 9, `PARTIAL` — 3, `BLOCKED` — 4, `EXTERNAL` — 2.

| Фаза | Задачи | READY |
|---|---|---|
| 0 · Разблокировка | T-01, T-02 | — (внешние) |
| 1 · Инфраструктура | T-03, T-04, T-05 | 3 |
| 2 · Модель оценки | T-06, T-07, T-08, T-09 | 3 |
| 3 · Журнал | T-10, T-11 | 2 |
| 4 · Веса и история | T-12, T-13 | 1 |
| 5 · Ролевая модель | T-14, T-15, T-16 | — |
| 7 · Демо-данные | T-17 | — |
| 9 · Приёмка | T-18 | — |

---

## Phase 0 · Разблокировка

### T-01 · Заведение ролей ПЗ в Keycloak `EXTERNAL`
| | |
|---|---|
| **Goal** | Четыре роли постановки существуют в realm и приходят в claim `roles` |
| **Requirements** | Состав ролей и их соотношение с существующими четырьмя — по ответу на `BA-36`. Именование по конвенции платформы (`GISBB_*`). Синхронизация с фронтендом (`forecast.roles.ts`) |
| **Affected modules** | Keycloak realm (вне репозитория), затем `app/roles.py` |
| **Dependencies** | Ответ БА `BA-36` |
| **Acceptance criteria** | Тестовый токен с новой ролью проходит проверку, роль видна в `Principal.roles` |
| **Not included** | Логика разграничения факторов — T-15 |

### T-02 · Получение разметки каталога от методолога `EXTERNAL`
| | |
|---|---|
| **Goal** | Каталог бруцеллёза содержит панель и ответственный орган по каждому фактору |
| **Requirements** | Разметка `tier` по 46 факторам (`BA-07`); машиночитаемая привязка «фактор → орган» (`BA-01`). Поле `responsible` остаётся справочным |
| **Affected modules** | `app/risk/data/brucellosis.json`, `app/risk/data/factor.schema.json` |
| **Dependencies** | Ответы БА и методолога |
| **Acceptance criteria** | У всех 46 факторов заполнены оба поля; `validate_catalog` не выдаёт проблем |
| **Not included** | Остальные 12 нозологий — по ответу на `BA-37` |

---

## Phase 1 · Инфраструктура доработки

### T-03 · Внедрить Alembic `READY`
| | |
|---|---|
| **Goal** | Изменения схемы `bb_risk` применяются миграциями, а не `create_all` |
| **Requirements** | Начальная миграция от текущего состояния схемы; `init_db` перестаёт полагаться на `create_all` для эволюции; сохранить ретраи подключения; учесть, что тесты идут на SQLite без схем |
| **Affected modules** | `app/db.py`, новый каталог миграций, `Dockerfile` или стартовый скрипт |
| **Dependencies** | — |
| **Acceptance criteria** | Миграция применяется и откатывается; добавление колонки доезжает до развёрнутой БД; тесты на SQLite проходят |
| **Tests** | Интеграционный: применить миграцию на копии схемы, проверить наличие колонки |
| **Not included** | Сами изменения модели — T-06, T-07 |

### T-04 · Исправить условие перезагрузки каталога `READY`
| | |
|---|---|
| **Goal** | Изменения внутри файла каталога попадают в БД при неизменном числе факторов |
| **Requirements** | Сейчас `catalog_loader.py:67-71` сравнивает количество записей и пропускает загрузку при совпадении. Заменить на сравнение содержимого — контрольная сумма файла либо версия в `infections.json`. Идемпотентность сохранить |
| **Affected modules** | `app/risk/catalog_loader.py`, возможно `app/risk/models.py` (поле версии) |
| **Dependencies** | — |
| **Acceptance criteria** | Изменение `tier` у фактора без изменения их числа приводит к обновлению строки в БД; повторный запуск без изменений файла не выполняет записи |
| **Tests** | Изменить поле в тестовом каталоге → перезапустить сид → значение обновилось; запустить дважды подряд → второй запуск без изменений |
| **Not included** | Сама разметка панелей — T-02 |

### T-05 · Подключить проверку каталога к сиду `READY`
| | |
|---|---|
| **Goal** | Некорректный каталог не загружается молча |
| **Requirements** | Вызывать `scoring.validate_catalog` перед записью; при наличии проблем — понятная ошибка в лог и отказ от загрузки этой нозологии. Функция уже написана (`scoring.py:174-207`), но не используется |
| **Affected modules** | `app/risk/catalog_loader.py` |
| **Dependencies** | — |
| **Acceptance criteria** | Каталог с дублем номера, весом вне 1–4 или бинарным фактором с баллами кроме 0 и 4 не загружается; сообщение содержит номер фактора |
| **Tests** | По одному негативному тесту на каждый вид проблемы; позитивный на всех 13 действующих каталогах |
| **Not included** | Исправление самих каталогов |

---

## Phase 2 · Модель оценки под Приложение 2

### T-06 · Период двумя датами `READY`
| | |
|---|---|
| **Goal** | Оценка хранит отчётный период как две обязательные даты |
| **Requirements** | Заменить `period: String(32), nullable` на `period_from` и `period_to` типа «дата», обязательные. Проверка `period_to >= period_from` на уровне API и БД. Формат отображения `DD-MM-YYYY` (BR-021, BR-022). `created_at` остаётся отдельно (BR-023) |
| **Affected modules** | `app/risk/models.py`, `app/risk/schemas.py`, `app/risk/service.py`, миграция |
| **Dependencies** | T-03 |
| **Acceptance criteria** | Запрос без периода отклоняется; `period_to < period_from` отклоняется; равные даты допускаются |
| **Tests** | Валидация обеих границ; равные даты; отсутствие поля |
| **Not included** | Согласование с периодичностью ролей (`Q-009`) |

### T-07 · Сохранение расчётных величин `READY`
| | |
|---|---|
| **Goal** | Результат расчёта сохраняется вместе с оценкой |
| **Requirements** | Добавить в `assessment`: интегральный индекс, полнота, скорректированный индекс, уровень, признак срабатывания триггера, число оценённых факторов, размер панели. Заполняются из результата `scoring.calculate_risk` в той же транзакции |
| **Affected modules** | `app/risk/models.py`, `app/risk/service.py`, миграция |
| **Dependencies** | T-03 |
| **Acceptance criteria** | После сохранения значения в БД совпадают с ответом API; уровень доступен без повторного расчёта |
| **Tests** | Сохранить оценку → прочитать из БД → сверить с результатом движка |
| **Not included** | Снимок весов и поведение при их правке — T-13, зависит от `Q-012` |

### T-08 · Ограничения целостности в БД `READY`
| | |
|---|---|
| **Goal** | Инварианты не зависят от приложения |
| **Requirements** | `CHECK` на балл 0–4 в `assessment_score`; `CHECK` на вес 1–4 в `factor`; ограничение длины наименования 250 символов (BR-037); `CHECK` на допустимые значения `type` и `tier` |
| **Affected modules** | `app/risk/models.py`, миграция |
| **Dependencies** | T-03 |
| **Acceptance criteria** | Прямая вставка балла 5 отклоняется хранилищем; существующие данные проходят миграцию |
| **Tests** | Негативный тест на каждое ограничение |
| **Not included** | Изменение диапазона веса при правке (`Q-036`) |

### T-09 · Территория регионом и районом `PARTIAL`
| | |
|---|---|
| **Goal** | Территория оценки соответствует Приложению 2 |
| **Requirements** | По ответу на `BA-17`: либо разделить `region_code` на регион и район с признаком «Обобщённо по региону», либо оставить один код КАТО. Формат отображения «Регион (район)» (BR-044) |
| **Affected modules** | `app/risk/models.py`, `app/risk/schemas.py`, миграция, возможно справочник территорий |
| **Dependencies** | T-03, ответ `BA-17` |
| **Acceptance criteria** | Район не может быть указан без региона; вариант «Обобщённо по региону» отличим от «район не выбран» |
| **Tests** | Позитивный и негативный на связку регион–район |
| **Not included** | Ведение самого справочника территорий |

---

## Phase 3 · Журнал

### T-10 · Выборка сохранённых оценок `READY`
| | |
|---|---|
| **Goal** | `GET /v1/risk/assessments` — список с фильтрами и пагинацией |
| **Requirements** | Фильтры: нозология, регион, период. Пагинация по конвенции сервиса (см. `/v1/forecast/runs`). Сортировка по умолчанию — по убыванию периода. В строке: нозология, территория, период, уровень, признак триггера. Доступ по `READ_ROLES` до появления ролей ПЗ |
| **Affected modules** | `app/routers/risk.py`, `app/risk/service.py`, `app/risk/schemas.py` |
| **Dependencies** | T-07 |
| **Acceptance criteria** | Каждый фильтр влияет на выдачу; пагинация соответствует принятой в сервисе; уровень берётся из сохранённых полей, а не пересчитывается |
| **Tests** | По тесту на каждый фильтр; пагинация; пустая выдача |
| **Not included** | Статус «Заполнено / Не заполнено» (T-11); ограничение по зоне ответственности (T-16); семантика фильтра по периоду (`Q-017`); поиск (`Q-018`) |

### T-11 · Статус и актуальная оценка `BLOCKED: Q-010, Q-014, Q-016, Q-017`
| | |
|---|---|
| **Goal** | Столбцы «Статус» и «Уровень» по Приложению 1 |
| **Requirements** | Правило порождения строк реестра — по ответу на `Q-010`; вычисление статуса относительно запрошенного периода; определение актуальной оценки — по `Q-016` |
| **Affected modules** | `app/risk/service.py`, `app/routers/risk.py` |
| **Dependencies** | T-10, ответы БА |
| **Acceptance criteria** | Строки со статусом «Не заполнено» появляются согласно принятому правилу |
| **Not included** | Цветовая индикация — фронтенд |

---

## Phase 4 · Веса и история

### T-12 · Таблица истории изменения весов `READY`
| | |
|---|---|
| **Goal** | Фиксация правок весового коэффициента |
| **Requirements** | Append-only таблица в схеме `bb_risk`: нозология, номер фактора, старое значение, новое значение, автор, дата и время. Минимальный состав нужен при любом ответе на `Q-027`; поле основания добавляется позже, если потребуется |
| **Affected modules** | `app/risk/models.py`, миграция |
| **Dependencies** | T-03 |
| **Acceptance criteria** | Запись создаётся, изменению и удалению не подлежит |
| **Tests** | Создание записи; отсутствие операций изменения |
| **Not included** | Эндпоинт правки — T-13 |

### T-13 · Правка веса `PARTIAL`
| | |
|---|---|
| **Goal** | Операция изменения веса с проверкой права и фиксацией в истории |
| **Requirements** | Эндпоинт правки; проверка права на сервере (до Phase 5 — по `WRITE_ROLES`, после — по роли Эксперта, BR-016); создание записи истории; поведение ранее сохранённых оценок — по ответу на `Q-012` |
| **Affected modules** | `app/routers/risk.py`, `app/risk/service.py` |
| **Dependencies** | T-12, T-07 |
| **Acceptance criteria** | Правка без права отклоняется сервером; одна правка порождает ровно одну запись истории; поведение прошлых оценок соответствует принятому варианту |
| **Tests** | Позитивный; негативный по роли; счётчик записей истории |
| **Not included** | Правка иных атрибутов фактора (`Q-043`); просмотр истории (`Q-027`) |

---

## Phase 5 · Ролевая модель

### T-14 · Роли ПЗ в сервисе `BLOCKED: T-01`
| | |
|---|---|
| **Goal** | Сервис знает о четырёх ролях постановки |
| **Requirements** | Добавить роли в `app/roles.py`, определить их место в `READ_ROLES` и `WRITE_ROLES` согласно ответу на `BA-36`. Имена — как в realm |
| **Affected modules** | `app/roles.py` |
| **Dependencies** | T-01 |
| **Acceptance criteria** | Токен с ролью органа проходит на запись, с ролью потребителя — только на чтение |
| **Tests** | По тесту на каждую роль |

### T-15 · Зона ответственности роли `BLOCKED: T-02, Q-001`
| | |
|---|---|
| **Goal** | Вычисление множества факторов, доступных роли |
| **Requirements** | Матрица берётся из данных каталога, не из кода (BR-042). Поведение для Эксперта — по `BA-05` |
| **Affected modules** | `app/risk/service.py`, новый модуль авторизации домена |
| **Dependencies** | T-14, T-02 |
| **Acceptance criteria** | Для каждой роли возвращается ожидаемое число факторов; изменение привязки в каталоге меняет результат без правки кода |
| **Tests** | По тесту на каждую роль; тест независимости от кода |

### T-16 · Разграничение на чтение и запись `BLOCKED: T-15`
| | |
|---|---|
| **Goal** | Реализовать §3.2 и §3.1 постановки |
| **Requirements** | Отклонение балла по фактору вне зоны ответственности на сервере (BR-014); фильтрация каталога и выдачи журнала по зоне (BR-012, BR-015); атомарность отказа — по ответу на `BA-24`; территориальное ограничение — по `BA-04` |
| **Affected modules** | `app/risk/service.py`, `app/routers/risk.py` |
| **Dependencies** | T-15, T-10 |
| **Acceptance criteria** | Запрос в обход интерфейса с чужим фактором отклоняется, частичных данных не остаётся; журнал не содержит записей вне зоны |
| **Tests** | Негативные тесты уровня API без участия интерфейса |

---

## Phase 7 · Демо-данные

### T-17 · Демо-заполнение с гейтом среды `BLOCKED: Q-029`
| | |
|---|---|
| **Goal** | Требование §3.2 при недоступности в промышленной среде |
| **Requirements** | Случайные баллы только по факторам своей роли; эндпоинт не регистрируется в промышленной конфигурации, а не просто скрыт в интерфейсе |
| **Affected modules** | `app/routers/risk.py`, `app/config.py` |
| **Dependencies** | T-06 |
| **Acceptance criteria** | В промышленной конфигурации функциональность недоступна, подтверждено тестом; баллы в диапазоне 0–4 |
| **Tests** | Недоступность в промышленной конфигурации; состав генерируемых факторов |

---

## Phase 9 · Приёмка

### T-18 · Сверка с реестром бизнес-правил `PARTIAL`
| | |
|---|---|
| **Goal** | Подтвердить соответствие реализации правилам ПЗ |
| **Requirements** | Пройти таблицу статусов в [business-rules.md](../01_PRODUCT/business-rules.md), перевести реализованные правила в ✅ со ссылкой на тест; явно перечислить нереализованное с причиной |
| **Affected modules** | документация, тесты |
| **Dependencies** | по мере готовности фаз |
| **Acceptance criteria** | Каждое реализованное правило имеет минимум один тест; расхождения перечислены |
| **Not included** | Правила фронтенда |

---

## Что можно взять в работу сегодня

| Задача | Что даёт |
|---|---|
| T-03 | Alembic — без него любое изменение схемы не доедет до среды |
| T-04 | Каталог начнёт обновляться при правке файла |
| T-05 | Каталог перестанет загружаться молча при ошибках |
| T-06 | Период по Приложению 2 |
| T-07 | Расчётные величины сохраняются — основа журнала |
| T-08 | Инварианты на уровне БД |
| T-10 | Журнал списком |
| T-12 | История изменения весов |

Восемь задач, ни одна не требует ответов бизнес-аналитика. Порядок: сначала T-03, T-04, T-05
(инфраструктура), затем остальные.

> Параллельно должны идти T-01 и T-02 — они не ваши, но блокируют пять задач.
